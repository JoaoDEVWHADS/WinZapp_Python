# Test tooling for the pip/venv workflow; see requirements.txt. Every package
# here must also be declared in pyproject.toml, and pinned there at a version
# these ranges accept (tests/test_requirements_in_sync.py).
pytest>=8.0
pytest-cov>=5.0
pytest-asyncio>=0.25
aiosqlite>=0.21
