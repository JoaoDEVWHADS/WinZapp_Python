[pytest]
testpaths = tests
pythonpath = client
asyncio_mode = auto
markers =
    slow: tests that run migration on large datasets
    integration: tests that require a real wx.App
    load: opt-in synthetic load tests (set WINZAPP_RUN_LOAD_TESTS=1)
